% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buzzEBMedAuto.R
\name{buzzEBMedAuto}
\alias{buzzEBMedAuto}
\title{Auto detecting variable types and selecting mediation effects (Automatic dispatcher)}
\usage{
buzzEBMedAuto(
  dataset,
  X,
  M,
  Y,
  scale = NULL,
  shape_m = NULL,
  rate_m = NULL,
  shape_y = NULL,
  rate_y = NULL,
  shape_a = NULL,
  rate_a = NULL,
  shape_b = NULL,
  rate_b = NULL,
  alpha_ind = NULL,
  beta_ind = NULL,
  tau_cprime = NULL,
  prec.m = NULL,
  prec.y = NULL,
  c.prime = NULL,
  taua = NULL,
  taub = NULL,
  ind.p = NULL,
  n_burnin = NULL,
  n_iter = NULL,
  thin = NULL
)
}
\arguments{
\item{dataset}{A data.frame containing the outcome, predictors, and mediators.}

\item{X}{A character string or character vector giving the name(s) of the
predictor variable(s) in \code{dataset}.}

\item{M}{A character vector giving the name(s) of the mediator variable(s)
in \code{dataset}.}

\item{Y}{A character string giving the name of the outcome variable in
\code{dataset}.}

\item{scale}{Logical; whether to standardize \code{X}, \code{Y}, and \code{M}
before fitting the model. If NULL, scaling behavior is determined internally.}

\item{shape_m}{Numeric. Shape parameter for the gamma prior on mediator
residual precisions (\code{prec.m[j]}). If NULL, a \code{dgamma(1, 0.001)}
prior is used (default).}

\item{rate_m}{Numeric. Rate parameter for the gamma prior on mediator
residual precisions. If NULL, a \code{dgamma(1, 0.001)} prior is used.}

\item{shape_y}{Numeric. Shape parameter for the gamma prior on the outcome
residual precision (\code{prec.y}). If NULL, a \code{dgamma(1, 0.001)}
prior is used (default).}

\item{rate_y}{Numeric. Rate parameter for the gamma prior on the outcome
residual precision. If NULL, a \code{dgamma(1, 0.001)} prior is used.}

\item{shape_a}{Numeric. Shape parameter for the gamma prior on the slab
precision of the \eqn{a} paths. If NULL, a \code{dgamma(1, 0.001)}
prior is used (default).}

\item{rate_a}{Numeric. Rate parameter for the gamma prior on the slab
precision of the \eqn{a} paths. If NULL, a \code{dgamma(1, 0.001)}
prior is used.}

\item{shape_b}{Numeric. Shape parameter for the gamma prior on the slab
precision of the \eqn{b} paths. If NULL, a \code{dgamma(1, 0.001)}
prior is used (default).}

\item{rate_b}{Numeric. Rate parameter for the gamma prior on the slab
precision of the \eqn{b} paths. If NULL, a \code{dgamma(1, 0.001)}
prior is used.}

\item{alpha_ind}{Numeric. Alpha parameter for the beta prior on the mediator
inclusion probability (\code{ind.p}). If NULL, a \code{Beta(3, 3)} prior
is used (default).}

\item{beta_ind}{Numeric. Beta parameter for the beta prior on the mediator
inclusion probability. If NULL, a \code{Beta(3, 3)} prior is used.}

\item{tau_cprime}{Numeric. Precision of the normal prior on the direct effects
of predictors (\code{c.prime}). If NULL, a \code{dnorm(0, 1.0E-6)}
prior is used (default).}

\item{prec.m}{Numeric. Initial value for mediator residual precisions.
If NULL, initialized at 1 (default).}

\item{prec.y}{Numeric. Initial value for the outcome residual precision.
If NULL, initialized at 1 (default).}

\item{c.prime}{Numeric. Initial value for the direct effects (\code{X -> Y}).
If NULL, initialized at 0 (default).}

\item{taua}{Numeric. Initial value for the slab precision of the \eqn{a} paths.
If NULL, initialized at 1 (default).}

\item{taub}{Numeric. Initial value for the slab precision of the \eqn{b} paths.
If NULL, initialized at 1 (default).}

\item{ind.p}{Numeric in (0, 1). Initial value for the mediator inclusion
probability. If NULL, initialized at 0.5 (default).}

\item{n_burnin}{Integer. Number of burn-in iterations for the MCMC sampler.
If NULL, 1000 iterations are used (default).}

\item{n_iter}{Integer. Number of MCMC iterations to sample after burn-in.
If NULL, 10000 iterations are used (default).}

\item{thin}{Integer. Thinning interval for MCMC samples.
If NULL, no thinning is applied (default = 1).}
}
\value{
An object of class \code{mcmc.list} containing posterior samples from JAGS.
}
\description{
This function automatically detects the data types of the outcome variables and dispatches
to the appropriate buzzMed model implementation.
}
\details{
The function prepares the data, constructs the corresponding JAGS model,
runs MCMC sampling, and returns posterior samples.

This function serves as a wrapper and automatic dispatcher. Based on whether
the mediator(s) and outcome are continuous or binary, it calls one of:
\code{buzzMYcont}, \code{buzzMcat},
\code{buzzYcat}, or \code{buzzMYcat}.

Internally, this function relies on \code{prepare_ebmed_data()},
\code{build_ebmed_model()}, \code{define_init_values()},
and \code{run_ebmed_jags()}.
}
